# Documentation cleanup — 2026-04-20

Conservative pass removing outdated version pins, stale "planned" labels,
and one duplicate module-table row. No structural reorganisation.

## Files touched (8)

- `README.md`
- `CLAUDE.md`
- `docs/index.md` — indirect (root CLAUDE.md reference updated)
- `docs/cli/commands.md`
- `docs/testing/stagelab.md`
- `docs/shorewall-nft/plugins.md`
- `docs/cli/override-json.md`
- `packages/shorewall-nft-stagelab/CLAUDE.md`

## Changes

| File | Lines deleted | Lines replaced/added | What |
|------|--------------|---------------------|------|
| `README.md` | 5 | 11 | Version badge v1.4.0→v1.10.0; stagelab/netkit added to Packages table and install snippet; Architecture section extended; test commands aligned with CLAUDE.md |
| `CLAUDE.md` | 0 | 2 | Removed stale `(NEW)` labels from stagelab/netkit; "3-package" → "4-package" |
| `docs/cli/commands.md` | 0 | 1 | Removed pinned `v1.4.0` from CLI intro line |
| `docs/testing/stagelab.md` | 5 | 4 | `conn_storm` tool: tcpkali → trafgen_pyconn; removed completed "Phase 4 planned" open item; "all four bundles" → five; removed "(Phase 3)" section label artifacts |
| `docs/shorewall-nft/plugins.md` | 1 | 1 | "Planned: full developer guide" → pointer to plugin-development.md (guide ships at 370 lines) |
| `docs/cli/override-json.md` | 2 | 0 | "Implementation seams (for the person wiring this up)" → "Implementation seams"; removed references to "TODO comments" that are now wired |
| `packages/shorewall-nft-stagelab/CLAUDE.md` | 2 | 1 | Removed duplicate `trafgen_pyconn.py` row (appeared at lines 35 and 51); `snmp_oids.py` bundle list: added missing `vrrp_extended` |

## Top 5 redundancy sources (before cleanup)

1. `trafgen_pyconn.py` duplicated verbatim in stagelab CLAUDE.md key-modules table (lines 35 and 51).
2. venv install snippet in README.md diverged from the authoritative version in CLAUDE.md (missing netkit + stagelab, wrong order).
3. "Phase 4 enterprise validation planned" in `docs/testing/stagelab.md` duplicated now-shipped content (D0-D4, P4-1..P4-7, audit-report — all in CHANGELOG 1.10.0).
4. SNMP bundle count inconsistency: stagelab.md said "four bundles" in tool table; actual bundle count is five (vrrp_extended added in 1.9.0/1.10.0).
5. Plugin developer guide cross-ref: plugins.md had "(Planned: full developer guide)" when plugin-development.md is a 370-line shipped doc.

## Top 5 outdated items fixed

1. README.md version badge: `v1.4.0` → `v1.10.0`.
2. `conn_storm` scenario tool: tcpkali (removed as default in 1.10.0) → trafgen_pyconn (pure-Python, no binary required).
3. "Phase 4 enterprise validation — DoS scenarios (D0-D4), P4-1..P4-7 … are planned": removed — shipped in 1.10.0 (commits bdd05e815, 91acb8694).
4. `docs/cli/commands.md` intro: hardcoded `v1.4.0` removed (number drifts on every release).
5. `docs/cli/override-json.md` "Implementation seams": reframed from future-work design notes to as-built description; "TODO comments" wording removed.

## Deferred items

- `docs/testing/live-firewall-test-plan.md` — multiple operator TODOs remain (pdns SNMP extend, endpoint-alias layer, --simlab-host flag, test-ACCEPT rule). All are legitimate open work items with real blockers; not orphaned. Leaving them in place.
- `docs/testing/point-of-truth.md` — references `iptables-save v1.4.21` (the iptables tool version, not shorewall-nft). Correct as-is.
- `packages/shorewalld/CLAUDE.md` "DNS architecture (shipped v1.4.0)" — historical anchor in an internal CLAUDE.md section heading. Not user-visible; leaving it.
- `docs/reference/commands.json` and `docs/reference/features.json` — `shorewall_nft_version: "1.0.0"` inside generated JSON. These appear to be auto-generated machine-readable references; regenerating them requires running the generation tooling, which is out of scope for a doc cleanup pass. Flag for the next release-bump commit.
- `docs/concepts/dynamic-routing.md:170` — "Add a tracking TODO for shorewall-nft to emit BGP TTL security automatically" — legitimate unimplemented feature, not orphaned.
